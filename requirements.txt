# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.3.0
scipy>=1.7.0

# OCR engines (choose at least one)
easyocr>=1.6.0  # Primary OCR engine
pytesseract>=0.3.8  # Fallback OCR engine

# NLP and PII detection
spacy>=3.4.0
# Download spacy model with: python -m spacy download en_core_web_sm

# Optional: Microsoft Presidio (uncomment if needed)
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# PDF processing (choose at least one)
PyMuPDF>=1.20.0  # Primary PDF engine (fitz)
pdf2image>=1.17.0,<2.0.0  # Fallback PDF engine (requires poppler)

# Web UI
streamlit>=1.15.0
streamlit-option-menu>=0.3.0

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.64.0

# Optional: YOLO for visual detection (uncomment if needed)
# ultralytics>=8.0.0

# Optional: Advanced computer vision (uncomment if needed)
# mediapipe>=0.9.0  # For face detection
# dlib>=19.24.0     # For face recognition

# System dependencies (install separately):
# - Tesseract OCR: https://tesseract-ocr.github.io/tessdoc/Installation.html
# - Poppler (for pdf2image): 
#   - Ubuntu/Debian: sudo apt-get install poppler-utils
#   - macOS: brew install poppler  
#   - Windows: Download from https://poppler.freedesktop.org/
